{
  "name": "angkor-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
  "start": "json-server --watch db.json --routes routes.json --host 0.0.0.0 --port $PORT --read-only"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
